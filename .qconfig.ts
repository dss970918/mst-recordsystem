import { getProjectPath, IQConfig } from '@jtl/q-scripts';

const qu: IQConfig = {
    mode: 'lib',
    publishConfig:{
        gitUrl: 'git@116.62.100.239:jtl3d-faces/picasso-recordsystem.git'
    }
};
export default qu;
